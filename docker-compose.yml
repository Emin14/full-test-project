services:
  backend:
    build:
      context: ./backend
      dockerfile: DockerFile.production
    ports:
      - 3000:3000
    container_name: travel-server
  
  frontend:
    build:
      context: ./frontend
      dockerfile: DockerFile.production
    ports:
      - 80:80
    container_name: travel-frontend
